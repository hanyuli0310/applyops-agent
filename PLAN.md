# ApplyOps v0.2 — 本地、可监督、可恢复的投递助手

> 状态：实施计划，尚未执行。本文不代表代码已修改、测试已通过或真实投递已成功。
> 评审基线：`hanyuli0310/applyops-agent`，`main`，commit `7f4d505c72b7dc463a43d189e36640924e03fedd`。
> 制定日期：2026-09-18（美国太平洋时间）/ 2026-09-19（UTC）。
> 执行入口：先读当前 `AGENTS.md`，再读本文件。若仓库已更新，重新核验问题，禁止覆盖已经修复的实现。

## 1. 产品目标与本轮边界

把现在面向熟悉仓库的操作者的 MCP 工具，收尾成可交给陌生用户内测的本地工具。用户不需要编辑 Markdown/JSON、管理进程或理解 MCP 协议，便能配置资料、准备申请、处理卡点和核对结果。已使用外部 AI 助手的用户，仍能通过原来的 MCP 入口驱动同一个核心。

本轮完成标准不是新增了多少页面，而是：

- 设置的资料、岗位偏好与简历确实进入执行链路。
- 对受支持的投递路线，未经有效授权，不产生最终提交动作。
- 未知、无法读取、未经验证的结果不伪装成成功。
- 遇到问题后有清楚的人工处理入口；恢复不会盲目重复投递。
- 一个新用户可以在不用开发者远程协助的情况下完成本地演示申请，并理解每一步的状态。

### 1.1 明确保留

保留 Python、Playwright、locator、答案/选择器/路由记忆、现有并发锁经验以及 MCP 外部接口。核心不内置模型、不要求新增模型 API key。新增的是本地服务、确定性的任务管理和用户界面，不是重新实现一个通用 LLM agent。

### 1.2 第一版不做

不做云端多用户 SaaS、订阅支付、账号体系、移动端、Electron/Tauri 桌面壳、全 ATS 覆盖、AI 改写全部简历、浏览器规避检测、验证码破解、自动读取 OTP、邮箱全量同步、外部 tracker 双向同步。Windows 不作为本轮必达平台；macOS 是首个验收平台，Linux 在实际验证后单独标注支持范围。

### 1.3 技术支持与平台许可分开

现有代码以 LinkedIn Easy Apply 为主要自动化路线，这不意味着平台允许自动化。LinkedIn 官方帮助页明确限制第三方软件对站内活动的自动化。因此：

- 默认开发、CI、演示使用本地 synthetic Demo ATS，不能使用真实求职账号和真实投递。
- LinkedIn 自动化适配保留为明确标识的实验能力，不能默认作为公开产品的安全/合规承诺。
- 对真实站点的自动化，在确认允许该使用方式和获得用户具体授权后才做有限验证；用户授权本身不替代平台许可。
- 用户手动粘贴岗位信息、整理材料、手动打开外部网页和记录进度，不依赖启用该自动化适配。
- 不宣传“每天少投一些就不会封号”，也不把限速实现当成平台许可。

## 2. 模型执行策略

这是开发 ApplyOps 的模型选择，不是要求 ApplyOps 产品内置这些模型。

### 默认组合

**Hy4 preview 主执行 + GLM-5.3 独立审查。**

用户截图中 Hy4 preview 显示限时免费 / 0.00x，因此适合先承担有明确验收标准的主线开发。Tencent 官方资料描述了其长流程软件工程与前端能力，也披露了复杂任务推理过久、过度自查等局限；这些是厂商说明，不是本项目的独立实测。

GLM-5.3 用于 M1、M2、M4 的独立审查和 Hy4 连续阻塞后的接手。Kimi-K3 可选用于前端截图反馈、交互打磨或复杂跨文件问题，不要求全程使用。没有证据证明截图中更高倍率的模型在本仓库一定更好。

截图中的 0.00x、0.79x、1.62x 等仅按用户当时界面理解，不推算成固定人民币费用、永久免费、无限调用或性能倍率。Balanced/Ultimate 的底层路由及 Max Mode 的具体语义未核实，不作为计划依赖。

### 用一个小任务选择主力

先让 Hy4 完成 M1 的字段 readback 回归测试与修复，检查：能否实际编辑文件和运行命令；是否保留原有语义；是否覆盖空读回与读取异常；是否没有删测试或放宽断言。通过后继续。若同一明确失败连续两轮没有新证据或有效进展，保存状态，切换 GLM-5.3。

不要用一次对话把所有阶段全部重写。每阶段独立 commit、测试报告和交接记录。不要让两个模型同时修改同一个工作区；独立审查默认只读，确需并行用隔离 worktree。

## 3. 用户体验目标

### 3.1 新用户流程

启动 → 本地自检 → 上传简历 → 核对资料 → 设置求职偏好 → 选择演示/受支持路线 → 准备一份申请 → 查看完整提交摘要 → 明确确认 → 查看结果证据。

初次只要求完成第一份申请必需的信息。可选资料允许跳过。简历文字抽取可以辅助填写，但抽取结果永远是待确认草稿；扫描件或无法解析的文件提供手工入口，不默默猜。工作授权、sponsorship、薪资、法律声明等由用户提供或确认，不能根据国籍、学校、历史推断生成。

### 3.2 四个页面

| 页面 | 首版功能 | 关键验收 |
|---|---|---|
| 资料与简历 | 上传/预览/设默认简历；编辑个人事实、常见答案、答案适用范围；查看自检与助手连接状态 | 用户无需输入绝对路径；显示实际文件名、版本和校验状态 |
| 岗位与偏好 | 粘贴 URL 或岗位文本、导入岗位列表；配置 title/地点/级别/排除条件；候选预览、排除原因、选择进入队列 | 同一条件在 UI、MCP、runner 中一致；改变规则后预览随之更新 |
| 待我处理 | 未知问题、资料冲突、登录失效、人工接管、提交审批、结果待核实 | 每条待办说明原因、关联岗位、可执行下一步 |
| 运行与记录 | 开始/暂停/停止；进度、失败原因、结果证据；CSV/JSON 导出 | 刷新页面不丢任务；待核实不计为成功；双击不会重复启动 |

界面优先清晰的表格、可读表单、状态标签与下一步按钮，不先做复杂统计图。所有页面都要有空态、加载态、错误态、断线态与禁用原因。使用真实 API 完成联调后才能标记页面完成，不能只交付静态 mockup。

### 3.3 不需要 AI 助手也能完成什么

本地 Demo ATS 与明确实现的确定性路线可以由本地 runner 执行，不要求调用模型。资料管理、岗位记录、排队、审批、人工恢复与导出也不依赖模型。

复杂未知路线需要外部 harness 时，要明确显示“需要连接支持 MCP 的助手”，提供连接检查和操作说明。不启动没有实际执行者的任务，不显示虚假的“AI 正在处理”。没有连接助手时保留人工完成与记录入口。

## 4. 目标架构：一个核心，一个浏览器拥有者

推荐技术组合：现有 Python 核心 + FastAPI 本地服务 + React/TypeScript/Vite 前端 + SQLite 任务账本。使用浏览器 SSE 或简单事件轮询更新进度，不引入 Redis、Celery、Kubernetes 或新的微服务。

```text
本地浏览器 UI ───────────────┐
外部 AI 助手 → stdio MCP 适配 ├→ 本地服务 → ApplicationService → 单一 worker → BrowserController
CLI / 旧 tools 兼容入口 ─────┘                       │
                                      状态机 / 授权 / 配额 / 任务账本
                                                  │
                                profile.md + memory.json + app.sqlite + artifacts
```

### 4.1 所有权与兼容

本地服务拥有唯一 Runtime、browser controller、任务队列与写入入口。MCP stdio 进程与 CLI 是薄客户端，不再各自创建会驱动浏览器的独立 Runtime。MCP 首次调用可按同一 data-root 检查/启动本地服务；多次启动必须由同一跨进程服务锁协调，不能创建多个 browser owner。

实际执行中的浏览器动作必须关联 run_id、application_id、tab/route，不允许另一个请求随意改变当前投递页面。UI 的暂停/审批请求可以同时到达，但 browser action 仍串行。

旧 `tools/auto_apply.py`、`cron_apply.py`、`op.py` 逐步改为共享 service 的兼容入口。禁止让旧路径继续直接写一套独立日志、创建第二个 Runtime、通过 `RUNTIME._browser` 私有注入逃开新的授权路径。迁移期间如果检测到旧 runner 持锁，明确提示先停止，不强杀、不自动抢浏览器。

尽量保持现有 MCP 工具名称与读取语义。确需改变提交相关行为时，显式版本化/弃用说明，旧接口不能继续凭 `acknowledged=True` 写出“已验证成功”。

### 4.2 数据归属：不做多套可写真相

| 数据 | 唯一权威来源 | 兼容方式 |
|---|---|---|
| 当前个人事实 | `data/profile.md`，通过 ProfileStore 读写 | 保留手工编辑；执行前读版本/哈希，变化时重新核对 |
| 常见答案、选择器、路由知识 | 现有 MemoryStore / `memory.json` | 保留原子写与锁；扩充 scope/provenance/revision；不复制一套可写答案表 |
| 岗位、申请、尝试、待办、审批、偏好、运行、配额、事件、材料元信息 | `data/app.sqlite` | 迁移旧申请与 guard 状态；旧 history API 从新账本投影 |
| 简历与证据文件 | `data/resumes/`、`data/evidence/` | 用 stable id、文件哈希和相对路径引用，不从前端接收任意服务器路径 |
| 旧 `application_log.json` / 旧申请 history | 迁移后只作备份或只读导出 | 禁止新旧双写；历史无证据记录不得自动升级成 verified |

引入 SQLite 是为了让任务、授权消费、配额预留等本地修改有事务边界，不是为了宣称它能与外部网站共同做原子提交。外部提交与本地记账之间仍有不确定窗口，必须通过 `submission_unknown` 和核实流程处理。

所有运行固定配置快照：profile_revision、answers_revision、preferences_revision、resume_id/hash、route_version。当前设置更新不悄悄改变已经批准的提交内容；受影响的申请要重新准备/确认。

### 4.3 建议新增模块，而非要求机械重排目录

```text
src/applyops/
  service.py                 # 统一业务接口
  jobs.py                    # 任务状态机与类型
  store.py                   # SQLite repository + migrations
  approvals.py               # 审批、策略和单次执行许可
  runner.py                  # 从个人脚本提取的确定性编排
  api/                       # 本地 HTTP/SSE 路由和访问控制
  mcp/                       # 原 MCP surface 的薄适配层
  ... existing modules ...
frontend/                    # React + TypeScript + Vite
tests/                       # 扩充现有测试根目录
```

测试统一放在现有 `tests/` 下。按职责拆分，不因本文件而制造空模块。`legacy/` 不导入、不重新上线。

## 5. 状态与提交协议

### 5.1 状态

建议 application 状态：

```text
queued → preparing → awaiting_input / awaiting_approval
                        ↓                  ↓
                    preparing          submitting
                                           ↓
                  submitted_verified / submission_unknown / failed
```

还需 `skipped`、`cancelled`、`manual_required`、`legacy_imported`。run 单独有 running/paused/stopped/completed；不要把 run 暂停与某份申请失败混为一谈。每次重试创建 attempt，不覆盖上一次尝试与证据。

`submission_unknown` 的含义：可能已经产生外部副作用，自动再次点击 Submit 被禁止。先重新读页面/申请状态或人工核实；明确发现未提交后，才允许新的、重新授权的尝试。不能把找不到成功文案当成“肯定失败”。

### 5.2 唯一提交路径

新增/抽取统一 `execute_submission()`，由核心服务负责：

1. 核对 job identity、route、页面、资料版本、实际附件与字段快照；无法验证则停下。
2. 校验用户来源的审批/有限策略授权，以及剩余数量、有效期、是否撤销。
3. 在本地事务中消费单次执行许可、预留配额、记录 attempt 的提交意图。
4. 在 browser action lock 下、实际发出动作前再次检查暂停/撤销与快照变化。
5. 执行受支持 route 明确界定的最终提交动作。
6. 获取结果证据并更新为 verified / unknown / failed；不明确时留作 unknown，不直接重试。

不能把批准发生在点击之后；不能让调用者写一句 reason 或传 acknowledged 布尔值等价于用户批准。

### 5.3 授权内容

一次审批至少绑定：application_id/job_key、route、完整实际字段快照、resume_id/hash、相关配置版本、expires_at、批准来源、单次 nonce、状态。

审批摘要由执行器从表单状态和准备数据生成；不只接受 LLM 自己写的自然语言总结。必须包含原本已预填的内容、所有表单阶段已经填写的内容和实际选中的附件；不知道的值标为未验证，不省略后自动发送。

简历更换、字段修改、岗位变更、关键页面变化会使旧批准失效。审批入口位于明确的本地用户 UI/人工 CLI，不提供给普通 MCP agent 一个“自行批准”的同等权限工具。

### 5.4 通用动作的绕过问题

只把名为 submit 的工具加 token 还不够。对受支持路线，普通 click、按钮 alias、CSS/ref、Enter、会提交的 Next、脚本触发及关联 form 的动作，都必须经过同一动作策略；不以 name.contains('Submit') 作为唯一控制。

不向不受信输入开放任意 JS 或浏览器底层执行通道。未知路线、未知有副作用按钮、无法可靠判定的最终动作，转为 manual_required，不能声称完全受保护。已知路线用合成表单覆盖 mouse、keyboard、custom button 与 re-render 等场景。

威胁边界必须诚实：这是对经本工具发出的行为的约束，不是对拥有同一 OS 用户 shell/文件权限的外部 agent 的强隔离。不得宣称同一用户权限下的恶意程序绝对无法绕过本地文件/浏览器控制。

### 5.5 有限自动模式

默认模式为逐份审阅确认。有限自动提交只在 M1—M4 安全测试通过后，且路线被允许自动化时启用。用户需在独立设置页明确开启。

策略必须限定路线、允许的岗位/公司/地点规则、简历版本、可复用的已确认答案、数量、时间窗口，并可随时撤销。空白、冲突、过期、超出 scope 的敏感答案、新问题、CAPTCHA、登录验证全部暂停或转人工，不推断。

先允许“自动发现/准备，集中审阅”，不为了演示而把受控自动模式变成默认全自动。

### 5.6 配额和取消

配额区分已验证成功数、已发出提交尝试数、待核实数。unknown 不能计入成功，但应保守保留已占用的尝试配额，避免利用不确定结果无限重试。额度在统一提交路径检查，不能仅靠可跳过的 preflight。

暂停发生在下一个安全动作边界；停止阻止后续新动作。已经发出的 HTTP/网页提交不能保证撤回。UI 不应显示“已取消发送”来掩盖已经在途的提交；必要时标为待核实。

## 6. 配置、简历和答案的细节

### 6.1 求职偏好

把 title 包含/排除、地点、远程偏好、经验级别、发布时间、公司黑名单、材料选择等放到统一设置中。不能把 Hanyu 的个人求职条件变成所有用户的默认值。

规则先做清楚可解释的 title 筛选，不先引入复杂推荐排序模型。可设置保守默认但必须让用户看到实际条件。岗位文本里没写的信息为 unknown，不能凭 title 推断“不需要 sponsorship”等事实。自动排除与仅提醒分开。

UI 预览、MCP discover/run、CLI 与计划任务调用同一 matcher。配置快照变更后要重算未批准候选；已经批准的申请不能静默套用新设置。

### 6.2 简历

上传后保存为受管理的文件，校验文件类型/大小，记录 resume_id、原名、hash、上传时间。第一版一份默认简历足够，同时保留将来多版本扩展的 id 设计。

彻底移除执行路径里对 `data/resume.pdf` 的硬编码依赖，兼容旧文件只作为迁移来源。浏览器上已有附件不能默认可信：核对文件名及页面可验证信息，无法证明与用户选择一致时重新上传或转人工。

### 6.3 答案 scope 与撤销

支持 global / company / application 范围、原始问题、用户确认来源、适用条件、最后修改时间与 revision。用户撤销或修改答案后，相关待提交申请重新检查，不能让旧批准继续有效。

对同样问题可批量答一次，但相似文字不自动合并不同语义；诸如“目前是否需要”和“现在或将来是否需要”、不同雇主的任职历史、不同币种/周期的薪资问题必须区分。

批量回答后只唤醒仍然相关、未过期、未确认成功的任务。OTP、CAPTCHA、密码不进入通用答案记忆，默认交给浏览器中的人工操作。

## 7. 五个实施里程碑

每个阶段有实现、测试与提交证据；下一阶段开始前先处理阻断缺陷。不以新增的文件数、测试数或截图数判定完成。

### M1 — 建立基线并修复可信执行的已知缺口

**任务**

- 记录实际 HEAD、工作区状态、依赖安装与原测试/lint 基线；工作区非干净时保护用户修改。
- 在临时 data-root 运行测试，禁止读取或修改真实 profile、cookies、申请历史。
- 为非空 expected + 空 readback、读回异常、trim 合法差异等场景补失败测试；修复 `fill_field` 的验证语义。
- 检查 select、checkbox/radio、upload 的返回值是否有“动作发生=字段正确”的混淆，逐步增加已验证/失败/无法验证三态。
- 给当前提交代码加回归测试：批准不能只限制事后记账；token 不能跨岗位复用；过期、重复、未批准、材料改变要拒绝。
- 统一实际附件来源；测试 UI/ProfileStore 选择的文件与 runner 上传参数一致。
- 把 submitted_unverified 从成功统计分离；先阻止已知错误路径，后续在 M2 完成事务化实现。
- 增加本地 Demo ATS，至少提供普通表单、预填表单、单选组、上传、成功响应和超时分支。

**验收**

新增回归测试在旧行为下真实失败、修复后通过；测试没有放宽原断言或跳过核心路径。没有新增真实站点提交。以 GLM-5.3 独立检查修复差异后再进入 M2。

### M2 — 统一配置、账本、授权与执行器

**任务**

- 实现 ApplicationService 与 SQLite schema/migrations；保留 profile/记忆各自权威来源。
- 实现 application/attempt/run/pending/approval/事件与配置快照。
- 统一 execute_submission、配额预留、完整摘要、证据落盘与 unknown 恢复规则。
- 本地服务成为唯一浏览器拥有者，MCP/CLI/旧 runner 接共享 service。
- 为 service 启动、单 worker、task claim、幂等请求建立并发测试。
- 实现迁移备份、失败恢复、重复迁移幂等；保守继承旧 guard 的已用额度与暂停状态。
- 历史没有证据的申请标记 legacy_imported 或 unknown，仍用于避免重复，但不作为新版本已验证成功率的分子。
- 完成 HTTP/API 数据契约；UI 与 MCP 不应各自定义不兼容的同名字段。

**验收**

同一申请从 UI API 和 MCP 同时发起只产生一个可执行 attempt；双击 approve、重复网络请求只消费一次批准。进程在发送前、发送中、已发送但未记账时退出，都落到正确且保守的恢复状态。旧数据可回滚，不丢答案、历史、额度和附件引用。

### M3 — 接通四页控制台与首次使用流程

**任务**

- 实现启动向导、自检、资料与简历页、岗位与偏好页、待处理页、运行与记录页。
- 先完成“新用户 → 一份 Demo 申请 → 实际表单 → UI 审批 → 证据”的端到端路径，再做样式打磨。
- 实现真实 API 联调、SSE/轮询事件、断线重连、页面刷新恢复。
- 接入外部助手状态检查；不依赖新增 API key；未连接时给清楚的人工/固定路线入口。
- 状态文字面向用户，例如“等待你确认附件”“页面提交结果尚未确认”，不只显示 raw traceback。
- 从中文起步，状态/文案集中管理；不要为了国际化平台而扩展本轮范围。

**验收**

本地 browser E2E 真正点通四页；验证 API 状态而非只截图。用户不输入文件路径即可使用简历。审批页展示完整字段、预填值与实际附件。任务在刷新后仍存在；加载/错误/空态可见；没有 mock data 假冒真实结果。

### M4 — 问题收集、恢复、队列与有限自动模式

**任务**

- 把 pending_questions 变成真实可操作待办；补齐 radio group 问题文本与 options，上下文不足转人工。
- 实现答一次、按 scope 复用、使旧批准失效、重新检查受影响申请。
- 支持暂停、停止、人工接管、登录失效后继续；记录 driver ownership，不与人工争抢同一标签页。
- 候选清单可预览后加入队列；搜索和执行能力分开标记。
- 去重优先 canonical URL / 平台 requisition id；company+title 只作可能重复提醒，不粗暴合并同公司不同地点/部门/编号的职位。
- 退避与重试有上限；只对明确可恢复操作重试。提交结果 unknown 不自动重发。
- 在允许的路线和通过验收的条件下实现有限策略授权；默认关闭。

**验收**

一个未知答案影响多份申请时，可回答一次并安全恢复；不同公司/不同 sponsorship 语义不串答案。取消后不再开始下一次提交。unknown 不重复点击。quota exhausted、breakers、session expired、route unsupported 各有可操作且不误导的 UI。

M4 结束由 GLM-5.3 做独立 review，重点找绕过路径、过期审批、跨岗位复用、重试副作用、双重账本和配置静默变化。

### M5 — 安装、打包、隐私、文档与内测验收

**任务**

- 实现清晰的 CLI 入口：`applyops doctor`、`applyops serve`、`applyops demo`、`applyops stop`（这些是本轮拟新增命令，当前不能假设已存在）。
- 提供 macOS 单一启动入口和可重复安装流程；产物包含构建好的前端，普通用户不需要 npm build 或前端开发服务器。
- Python/浏览器依赖可自动检查并经用户确认安装；遇到网络、端口、权限问题提供准确诊断。
- data-root 不绑定 site-packages 路径；明确迁移与备份位置。保留现有仓库 data 兼容能力，不能把用户资料写入 Python 安装目录。
- API 默认只监听 loopback；严格 Host/Origin、session auth 与 CSRF；不使用 wildcard CORS，不开放任意文件读取/上传路径，不把 CDP 端口暴露到公网。
- 区分用户审批会话与 MCP 普通工具权限；密钥与敏感数据不进入日志、错误上报、测试 fixture、截图展示或 Git。
- 提供资料/记忆/历史导出、清理登录状态、删除本地资料与诊断脱敏入口。警告删除历史会失去部分去重依据；删除与 revoke 行为先停止新提交。
- 文档明确哪些内容会通过外部 harness 传给模型，本地保存不等于所有处理都本地执行。
- 建立 Python unit/integration、真实多进程并发、UI E2E、前端 typecheck/build 的 CI。保留可靠的锁测试，不替换成无法发现进程竞态的 mocks。
- 根 `AGENTS.md` 更新为单一接手入口：如何启动、运行测试、下一步、禁止事项；本 `PLAN.md` 保留计划与阶段状态，另一个小状态文件只记录执行事实，不复制整套规则。
- 文档列出实际支持/实验/仅人工的 route matrix；标明平台许可限制，不夸大成功率。

**验收**

一台干净 macOS 环境可按说明安装并运行演示，不手改 JSON/MCP 路径；前端构建产物可用。Linux 只有验证过的范围才写 supported。CI 结果可复核；缺能力的测试标为 BLOCKED/NOT RUN，不能标为通过。

邀请 3–5 名未参与开发的用户完成本地演示流程。记录首次到达审核页耗时、需要帮助次数、卡点、是否正确理解结果和下一步。建议目标：至少 4/5（或仅 3 人时 3/3）无需开发者干预完成 Demo；这是内测目标，不是已达到的指标。真实站点质量只统计明确授权且允许的运行，报告分母、路线版本及 unknown 数量。

## 8. 最小测试矩阵

| 类别 | 必测场景 | 预期 |
|---|---|---|
| 输入验证 | 非空预期读回空；读回抛错；原样文本；可接受格式差异 | 空/异常不被误报成功 |
| 提交授权 | 无审批、过期、撤销、重复、跨 job、字段/附件已改 | 都不触发最终提交 |
| 动作入口 | mouse、Enter、CSS/ref alias、自定义按钮、final Next | 受支持路线均通过同一策略；未知动作转人工 |
| 结果 | 可靠成功证据、明确拒绝、超时、登录重定向 | verified / failed / unknown 清晰分开 |
| 崩溃 | 许可预留前后、外部发送后但本地未写结果 | 不自动重发，不丢历史 |
| 配额 | 最后一个 slot 的并发竞争；unknown 占用；重启 | 不超发，不靠重启复位额度 |
| 配置 | UI/MCP/runner 相同偏好；修改 profile.md | 一致读取；快照变化导致重新确认 |
| 材料 | 用户选择 A，页面曾保留 B；文件丢失/哈希变更 | 不误用 B，缺失有提示 |
| 记忆 | 通用/公司/单次；撤销；近似问题不同语义 | 不串用、不继续使用撤销答案 |
| 队列 | 同一职位跟踪链接；同公司同 title 不同 requisition | 真重复合并，疑似重复提醒不硬删 |
| 恢复 | 待办回答、浏览器失联、人工接管、暂停/停止 | 用户看得懂、可继续、无并发争抢 |
| 迁移 | 旧资料、旧 guard、未知历史、重复迁移、迁移失败 | 原数据保留，可回退，不造 verified |
| UI | 新用户端到端、空态、错误、刷新、双击、断线重连 | 真实后端状态，无假完成 |
| 本地安全 | 错 Origin/Host、无 session、伪造批准、路径穿越、日志含密钥 | 拒绝越权，敏感数据不泄露 |

不要只写“新增 100 个测试”。报告每类真实命令、退出码、失败项、未执行原因与测试产物。不能通过降低安全要求来适配测试。

## 9. 发布与验收指标

必须分别报告：进入候选池、进入队列、尝试填写、等待人工、发出提交、已验证成功、待核实、明确失败、跳过。不能把找到 100 个岗位写成投了 100 份。

关键用户指标：首次到审核页耗时；单份申请人工打断次数；从待办恢复的比例；配置/材料错误次数；未确认即提交次数；重复提交次数；有证据的成功数与全部尝试数。

只在有可靠分母和证据时报告成功率。Demo ATS 的通过率不冒充真实 ATS 成功率；历史 imported 数据不混入新版本验证统计。

公开发布之前必须完成：P0 缺陷清零、迁移可回退、平台/路线支持范围标明、默认安全模式、安装验收、隐私说明。对外宣称“可用的真实自动投递”还必须至少完成一个使用方式允许且实际验证的真实路线；只有 Demo 通过时，只能发布为演示/内测或人工辅助版，不能把它说成生产自动投递已经验证。3–5 人内测不是大规模可靠性证明；保留问题反馈入口和路线开关。

## 10. Coding agent 执行合同

### 10.1 首次执行

1. 先读取实际仓库，记录 HEAD、工作区、现有入口；不要仅凭本文重建代码。
2. 保护已有修改和真实 data；使用 feature branch，例如 `feat/local-console-v02`。测试必须使用临时目录与合成资料。
3. 按 M1→M5 顺序实施，每个 milestone 分小步 commit。可以本地 commit，不自动 push、不发布、不 force-push，不读取真实浏览器凭据或替用户实际申请职位。
4. 第一阶段先做 readback 回归测试与修复，用这个结果校准模型是否适合继续主执行。
5. 本文与 `AGENTS.md` 发生架构冲突时明确记录本轮选择：保留无模型核心，新增共享本地 service/worker；不恢复 legacy LLM loop。不要悄悄删掉安全不变量。

### 10.2 每阶段完成报告

- 修改了哪些真实文件、为什么改。
- 哪些现有行为保持兼容，哪些 API 有意变化。
- 运行了哪些命令、退出码及产物路径。
- 哪些验收项通过、失败、未运行；不能把“准备好了测试”当“跑过测试”。
- 残留风险、回滚方式、下一阶段第一项任务。
- 当前 commit id，当前模型/harness，是否触及真实账户（本计划默认应为否）。

### 10.3 停止与升级条件

出现不可恢复的数据迁移风险、需要真实凭据/真实外部投递/付款/发布、平台许可未明确、无法验证是否已提交、需要超出范围的新基础设施、同一关键失败两轮无进展时，停止该部分并报告。可以继续独立、无副作用的任务，不得为了保持“连续执行”降低授权或验收标准。

在需要切换模型时更新 `IMPLEMENTATION_STATUS.md`，只记录当前 milestone、已通过证据、失败命令、工作区变化、下一步；接手模型先读取并核验，不重复重构整个项目。

### 10.4 禁止的完成方式

- 不允许直接恢复 legacy 前端与旧 agent loop 来掩盖新核心尚未联通。
- 不允许 HTML 按钮假数据演示替代真实 API。
- 不允许删除/跳过原测试、放宽断言、写死测试环境返回成功。
- 不允许改名状态却仍将 unverified 计入成功。
- 不允许用 reason、acknowledged 或 LLM 生成的文本当用户批准。
- 不允许假设有 SQLite 就消除了外部提交的不确定性。
- 不允许把整个计划一次性“大重构”后不给阶段证据。

## 11. 推荐启动 prompt

```text
阅读当前仓库的 AGENTS.md 和根目录 PLAN.md。按 PLAN.md 实施 ApplyOps v0.2，不只是补一个静态前端。

先记录实际 HEAD 和工作区，保护已有修改与 data；在 feature branch 上工作。保留 MCP、现有浏览器/记忆核心和无模型 API key 设计，不恢复 legacy agent/LLM loop。

按 M1→M5 顺序推进。现在先完成 M1 中 fill_field 的 readback 回归测试与修复，覆盖非空预期读回空、读取异常和合法格式差异；运行真实测试，再继续该阶段的授权、结果与简历问题。

UI、MCP、CLI、定时入口最终必须接同一个本地 service/worker；提交必须先授权、再执行、后验证。unknown 不能计成功或盲目重投。测试仅用临时 data-root 与本地 Demo ATS，不登录真实账号、不真实投递。

每阶段完成后运行验收，记录实际命令、结果、未运行项和 commit，更新 IMPLEMENTATION_STATUS.md。允许本地提交，不自动 push、发布或 force-push。M1/M2/M4 完成后留出独立审查点，未清零阻断问题不进入下一阶段。无法继续时写清阻塞，不伪造完成，不降低安全要求。
```

## 12. 来源与核验范围

以下源码已用于本轮静态评审；没有在本环境运行这个仓库的测试，也没有操作真实投递账号：

- 仓库主线 commit：`7f4d505c72b7dc463a43d189e36640924e03fedd`。
- `AGENTS.md`：现有无模型/MCP 定位、入口、数据与操作说明。
- `src/applyops/browser.py`：字段读回与浏览器动作实现。
- `src/applyops/mcp/tools.py`：提交前请求确认与提交后记录的现有分离。
- 同一基线先前读取的 `tools/auto_apply.py`、`guardrails.py`、`runtime.py`、`profile.py` 等用于定位迁移点；执行前逐项重新核验。

官方参考（链接仅用于核验，不是要求 agent 联网调用模型）：

- Tencent Hy4 preview 官方仓库 README：`https://github.com/Tencent-Hunyuan/Hy4-preview`，含软件工程能力说明与 known limitations。
- Z.ai GLM-5.3 官方发布：`https://z.ai/blog/glm-5.3`。
- Moonshot Kimi K3 官方技术说明：`https://www.kimi.ai/blog/kimi-k3`。
- SQLite atomic commit：`https://sqlite.org/atomiccommit.html`。本地事务边界不能被误用为外部网站 exactly-once 保证。
- LinkedIn Automated activity：`https://www.linkedin.com/help/linkedin/answer/a1340567`。
- LinkedIn Prohibited software and extensions：`https://www.linkedin.com/help/linkedin/answer/a1341387`。

模型分工和架构/里程碑是本计划的建议，不是这些厂商对本项目的承诺。用户截图所示免费与倍率状态可能变化，实际使用前以客户端为准。
